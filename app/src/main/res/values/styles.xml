<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
<!--        <item name="android:statusBarColor" tools:targetApi="m">@color/bg</item>-->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <style name="shadow">
        <item name="android:elevation">16dp</item>
        <item name="android:outlineSpotShadowColor" tools:targetApi="p">@color/shadow</item>
    </style>

    <style name="default_padding">
        <item name="android:paddingStart">@dimen/default_padding</item>
        <item name="android:paddingTop">@dimen/half_padding</item>
        <item name="android:paddingEnd">@dimen/default_padding</item>
        <item name="android:paddingBottom">@dimen/half_padding</item>
    </style>

    <style name="default_margin">
        <item name="android:layout_marginStart">@dimen/default_padding</item>
        <item name="android:layout_marginTop">@dimen/half_padding</item>
        <item name="android:layout_marginEnd">@dimen/default_padding</item>
        <item name="android:layout_marginBottom">@dimen/half_padding</item>
    </style>

</resources>